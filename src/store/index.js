import { createStore } from "vuex";
import credentials from "./credentials";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    credentials
  },
});
